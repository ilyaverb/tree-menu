# Tree menu
Tree menu via template tags.

Install dependencies with poetry:
```
poetry init
poetry install
```
and activate venv:
```
poetry shell
```


superuser credentials (src/db.sqlite3):
```
login: admin
password: 0000
```
